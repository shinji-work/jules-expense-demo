/**
 * 共通で使用する UI コンポーネントのスタイルクラスを定義します。
 * Tailwind CSS のユーティリティクラスをまとめ、デザインの一貫性を保ちます。
 */
export const cardClass =
  'rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200';

/**
 * コンポーネント内のセクションタイトルに適用する基本スタイルです。
 */
export const headingClass = 'text-2xl font-semibold text-slate-900';

/**
 * 入力フィールドに適用する基本のスタイルクラスです。
 */
export const inputClass =
  'mt-1 w-full rounded-lg border border-slate-200 bg-white px-4 py-2 text-slate-900 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-1';

/**
 * 入力フィールドに関連付けるラベルのスタイルクラスです。
 */
export const labelClass = 'block text-sm font-medium text-slate-700';

/**
 * フォーム送信などの主要アクションに使用するボタンのスタイルクラスです。
 */
export const primaryButtonClass =
  'inline-flex items-center justify-center rounded-lg bg-blue-600 px-5 py-2 text-sm font-semibold text-white shadow hover:bg-blue-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2';
